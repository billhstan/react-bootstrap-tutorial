name: S3 bucket
 
on:
 push:
   branches: master
 pull_request:
   branches: master
 
jobs:
 build:
   runs-on: ubuntu-latest
   strategy:
     matrix:
       node-version: 10.x
   steps:
   - uses: actions/checkout@v2